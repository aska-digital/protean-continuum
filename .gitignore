# runtime data — generated by scan/sync, never committed (live instance state)
server/data/

# environment / secrets — never commit
.env
.env.*

# python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# sqlite strays
*.db-wal
*.db-shm

# os noise
.DS_Store

# generated runtime content — not committed (build artifact, synthetic fixture is server/fixtures/review-queue.json)
pages-root/review-queue.json
